#look at mezzanine example, some defaults stored in database, some picked up from settings.py file

#jsPlumber → django default store location JSPLUMB_STORAGE ? use MEDIA if not found.


#Assumptions
'''
- Use only Perimeter anchor
- No need for Diamond Shape, no need for circle or square

- No need to auto-generate endpoints by joining elements, use manually generated enpoints only
- draggable applies to all elements

'''